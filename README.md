# My Personal Website

My personal site built using [React](https://reactjs.org/)

Configuration
- Do not hardcode secrets in source code or documentation.
- Set required values via environment variables (e.g., API_KEY).
- Create a .env file based on .env.example and populate your values.
- Ensure .env is listed in .gitignore to prevent committing secrets.